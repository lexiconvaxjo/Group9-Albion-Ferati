@model List<ForumProject.Models.Reply>


@*<table>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReplyContent)
        </td>
    </tr>
}
</table>*@

<div class="form-horizontal">
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    @foreach (var item in Model)
    {
        <div class="form-group">
            @Html.LabelFor(modelItem => item.ReplyContent, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DisplayFor(modelItem => item.ReplyContent, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(modelItem => item.ReplyContent, "", new { @class = "text-danger" })
            </div>
        </div>
    }
</div>
